# Local message store (databases + downloaded media)
whatsapp-bridge/store/
*.db

# Local Claude Desktop config (machine-specific)
claude_desktop_config.json

# Logs
*.log

# Compiled bridge binary (built by setup.ps1 / go build)
whatsapp-bridge/whatsapp-bridge.exe
whatsapp-bridge/whatsapp-bridge
*.exe

# Diagnostic scratch
tlstest/
nettest/

# Python virtual env / caches (uv sync, pytest)
.venv/
__pycache__/
*.pyc
.pytest_cache/
